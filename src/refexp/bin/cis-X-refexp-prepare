#!/usr/bin/env bash

CIS_X_REFEXP_HOME=$(realpath $(dirname $0)/..)

CONFIG=$1
RESULTS_DIR=$2

if [ $# -lt 2 ]; then
    echo "Usage: $(basename $0) <config> <results-dir>"
    exit 1
fi

perl $CIS_X_REFEXP_HOME/src/cis-X.refexp.step1.pl $CONFIG $RESULTS_DIR
